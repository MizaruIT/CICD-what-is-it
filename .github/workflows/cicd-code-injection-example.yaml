
name: Test CI (VULNERABLE)

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ⚠️ VULNÉRABLE : interpolation directe d'entrées non fiables
      - name: Echo PR metadata (VULNERABLE)
        run: |
          echo "PR ID: ${{ github.event.number }}"
          echo "PR Title: ${{ github.event.pull_request.title }}"
